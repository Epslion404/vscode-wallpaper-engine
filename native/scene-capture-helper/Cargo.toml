[package]
name = "vwe-scene-capture-helper"
version = "0.1.0"
edition = "2024"
license = "MIT"
publish = false

[dependencies]
windows-capture = "=2.0.1"

[profile.release]
codegen-units = 1
lto = "thin"
opt-level = "s"
panic = "abort"
strip = "symbols"
