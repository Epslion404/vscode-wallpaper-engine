# Release 状态

## v0.1.1

当前推荐版本，包含 modern UI shell 黑屏回归修复、Workbench 注入协议 `3` 和最新文档。

安装文件：`vscode-wallpaper-engine-0.1.1.vsix`

## v0.1.0

**严重缺陷，不可用。**

已知问题：更换壁纸或重载窗口后，壁纸可能只在 VS Code 启动瞬间出现，随后被 Workbench 的现代 UI shell 不透明背景覆盖。请勿继续使用 `vscode-wallpaper-engine-0.1.0.vsix`，改用 `v0.1.1`。
